services:
  - type: web
    name: ids-backend
    runtime: python3
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/server.py --demo --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11